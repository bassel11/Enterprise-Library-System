@{
    Layout = null;
    ViewData["Title"] = "Create Account - Enterprise Library";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        .register-side {
            background: url('https://images.unsplash.com/photo-1481627834876-b7833e8f5570?q=80&w=2000&auto=format&fit=crop') center/cover no-repeat;
            min-height: 100vh;
            position: relative;
        }

            .register-side::after {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0, 0, 0, 0.7);
            }

        .register-content {
            position: relative;
            z-index: 1;
        }
    </style>
</head>
<body>
    <div class="container-fluid p-0">
        <div class="row g-0">
            <div class="col-lg-6 d-none d-lg-flex register-side align-items-center justify-content-center">
                <div class="text-center text-white register-content p-5">
                    <i class="bi bi-journal-richtext display-1 mb-4"></i>
                    <h1 class="fw-bold display-4">Join Our Library</h1>
                    <p class="lead mt-3">Access thousands of books. Secure. Fast. Reliable.</p>
                </div>
            </div>

            <div class="col-lg-6 d-flex align-items-center justify-content-center" style="background-color: #ffffff;">
                <div class="w-100 p-5" style="max-width: 450px;">
                    <div class="text-center mb-5">
                        <i class="bi bi-person-plus fs-1 text-primary"></i>
                        <h2 class="fw-bold mt-2">Create an Account</h2>
                        <p class="text-muted">Fill in your details to get started</p>
                    </div>

                    @if (ViewBag.ErrorMessage != null)
                    {
                        <div class="alert alert-danger shadow-sm border-0 border-start border-5 border-danger">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i> @ViewBag.ErrorMessage
                        </div>
                    }

                    <form asp-controller="Auth" asp-action="Register" method="post">
                        @Html.AntiForgeryToken()

                        <div class="form-floating mb-4">
                            <input type="text" class="form-control rounded-3" id="username" name="username" placeholder="Choose a Username" required>
                            <label for="username"><i class="bi bi-person me-2"></i>Username</label>
                        </div>
                        <div class="form-floating mb-4">
                            <input type="password" class="form-control rounded-3" id="password" name="password" placeholder="Create a Password" required>
                            <label for="password"><i class="bi bi-lock me-2"></i>Password</label>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 py-3 rounded-3 fw-bold shadow-sm transition">
                            Register <i class="bi bi-check2-circle ms-2"></i>
                        </button>
                    </form>

                    <div class="text-center mt-4">
                        <p class="text-muted">Already have an account? <a href="/Auth/Login" class="text-decoration-none fw-bold">Sign In here</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>